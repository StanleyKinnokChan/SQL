WITH CTE AS (
    SELECT 
        ROW_NUMBER() OVER (ORDER BY LAT_N) as ROWNUM, 
        LAT_N 
    FROM STATION
    )
SELECT 
    CAST(LAT_N AS DECIMAL(10,4)) 
FROM CTE
WHERE ROWNUM =(SELECT (MAX(ROWNUM)+1)/2 FROM CTE)